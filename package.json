{
  "name": "omni-landing",
  "private": true,
  "scripts": {
    "start": "eleventy --serve",
    "build": "omnibuilder",
    "preview": "npm run build && serve dist"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@11ty/eleventy-plugin-syntaxhighlight": "^5.0.2",
    "omni-builder": "file:../omni-builder",
    "serve": "^14.2.6"
  }
}
